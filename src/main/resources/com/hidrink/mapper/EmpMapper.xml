<?xml version="1.0" encoding="UTF-8" ?>     <!--固定头-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hidrink.mapper.EmpMapper">

    <delete id="deleteEmp">
        delete from emp
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateEmpById">
        update emp
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="img != null and img != ''">
                img = #{img},
            </if>
            <if test="entryTime != null">
                entry_time = #{entryTime},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="getEmp" resultType="com.hidrink.pojo.Emp">
        select * from emp
        <where>     <!--根据情况，mybatis自动调整确认是否加where或过滤and-->
            <if test="name != null and name != ''"> <!--根据条件，mybatis自动调整确认是否需要添加该条件-->
               and username like concat('%', #{name}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
               and entry_time &gt;= #{startDate} <!-- &gt;为大于号  &lt;为小于号 -->
            </if>
            <if test="endDate != null and endDate != ''">
               and entry_time &lt;= #{endDate}
            </if>
        </where>
        order by update_time desc
    </select>

</mapper>